import { Page } from "page.slint";
import { Translator } from "../../lang/translator.slint";
import { Lang } from "../../lang/lang.slint";
import { Translator,TranslatorData } from "../../lang/translator.slint";
export component TunnelPage inherits Page {
    in-out property <Translator> lang_trans:Lang.translator;
    header: lang-trans.tunnel;
}